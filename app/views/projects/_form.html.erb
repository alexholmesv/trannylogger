<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form>
<div class="form-inline">
<div class="form-group">
  <div class="field">
    <%= f.label :client_id %><br>
    <%= f.select :client_id, @clients.map { |m| [m.name, m.id] }%>
    <%= link_to 'New Client', new_client_path %>
  </div>
  <div class="field">
    <%= f.label :translator_id %><br>
    <%= f.select :translator_id, @translators.map { |m| [m.name, m.id] }%>
    <%= link_to 'New Translator', new_translator_path %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date_received %><br>
    <%= f.date_select :date_received %>
  </div>
  <div class="field">
    <%= f.label :words %><br>
    <%= f.number_field :words %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="field">
    <%= f.label :extras %><br>
    <%= f.text_field :extras %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :sent, "Has this project been sent to the client?" %>
    <%= f.check_box :sent %>
  </div>
  <div class="field">
    <%= f.label :invoice_number, "Número de boleta o factura" %><br>
    <%= f.text_field :invoice_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  </div>
  </div>
</form>
<% end %>
